移动指令：
	协议号
	指令发出时的客户端帧数	nSendFrame
	方向			nDirection
	持续帧数		nFrame
	共1+8+1+1 = 11B
玩家的上次活动帧数：nLastFrame，用来限制回滚
会改写nLastFrame的行为：
	接收到的所有客户端数据包（TCP的顺序性）
	服务端的强制行为：
		遇障碍转为站立姿态（理论上这不可能发生，发生时通知客户端，强制同步位置）
		被子弹击中
	总的原则：单纯的移动行为可以由客户端控制，可以回滚；		
关于帧数
	所有帧数以服务端为准