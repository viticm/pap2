1、只针对npc普通待机动作对动作是否循环进行处理；
2、npc普通待机动作ID：30~35；
3、当npc只有一个普通待机动作时，npc的该动作循环类型设为1
当npc有一个以上普通待机动作时，npc的所有普通待机动作循环类型设为0
4、当npc开始只有1个普通待机动作，当为npc增加1个或一个以上普通待机动作时，npc的所有普通待机动作循环类型均设置为0；
当npc开始有多个普通待机动作，当把npc的普通待机动作删减为1个时，npc的普通待机动作循环类型设为1
5、对现有的npc刷新一次，区分多个普通待机和只有1个普通待机的情况，分别对循环类型设置。